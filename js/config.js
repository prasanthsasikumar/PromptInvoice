/* Optional cloud sign-in. Leave both empty to run purely in the browser (local mode).
   To enable "Sign in with your work email", create a free Supabase project, run
   supabase/schema.sql in its SQL editor, and paste the project URL and anon key here.
   The anon key is safe to publish — row-level security in the schema protects the data. */
window.PI_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
};
